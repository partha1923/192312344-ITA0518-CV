# -*- coding: utf-8 -*-
"""qo5

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1LkzO6EV06IF3h57KFy344UgPcBaW0Yzh
"""

import cv2
import matplotlib.pyplot as plt

import cv2

# Load the image
image = cv2.imread('/content/iok.jpg')   # Put your image file here

# Check if image is loaded
if image is None:
    print("Error: Image not found!")
else:
    print("Image loaded successfully")

gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)

face_cascade = cv2.CascadeClassifier(
    cv2.data.haarcascades + 'haarcascade_frontalface_default.xml'
)

faces = face_cascade.detectMultiScale(
    gray,
    scaleFactor=1.3,
    minNeighbors=5
)

print("Number of persons detected:", len(faces))

for (x, y, w, h) in faces:
    # Draw rectangle
    cv2.rectangle(image, (x, y), (x+w, y+h), (255, 0, 0), 2)

    # Add label
    cv2.putText(image, "Person Detected",
                (x, y-10),
                cv2.FONT_HERSHEY_SIMPLEX,
                0.6,
                (0, 255, 0),
                2)

plt.imshow(cv2.cvtColor(image, cv2.COLOR_BGR2RGB))
plt.title("Surveillance Output")
plt.axis('off')
plt.show()

